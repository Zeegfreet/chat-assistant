type: object

required:
  - id
  - createdAt
  - updatedAt

properties:
  id:
    type: integer
    example: 1
    
  createdAt:
    type: string
    format: date-time

  updatedAt:
    type: string
    format: date-time